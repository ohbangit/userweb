/**
 * 앱 전역 설정 상수
 *
 * 수치를 조정할 때 이 파일만 수정하면 됩니다.
 * UI 동작에 영향을 주는 "매직 넘버"를 의미 있는 이름으로 관리합니다.
 */

// ---------------------------------------------------------------------------
// TanStack Query — staleTime
// ---------------------------------------------------------------------------

/** 기본 staleTime (5분) — 자주 바뀌는 데이터 (스케줄, 배너 등) */
export const QUERY_STALE_TIME_DEFAULT = 1000 * 60 * 5

/** 긴 staleTime (10분) — 잘 바뀌지 않는 데이터 (토너먼트 목록 등) */
export const QUERY_STALE_TIME_LONG = 1000 * 60 * 10

// ---------------------------------------------------------------------------
// 배너 캐러셀
// ---------------------------------------------------------------------------

/** 자동 슬라이드 전환 주기 (ms) */
export const BANNER_CAROUSEL_INTERVAL_MS = 4000

/** 터치 스와이프로 인식하는 최소 이동 거리 (px) */
export const BANNER_CAROUSEL_SWIPE_THRESHOLD_PX = 40

/** 대형 이미지로 분류하는 최소 너비 (px) — 대각선 디테일 오버레이 적용 기준 */
export const BANNER_LARGE_IMAGE_MIN_WIDTH_PX = 960

/** 대형 이미지로 분류하는 최소 높이 (px) */
export const BANNER_LARGE_IMAGE_MIN_HEIGHT_PX = 540

// ---------------------------------------------------------------------------
// 토스트
// ---------------------------------------------------------------------------

/** 토스트 기본 자동 닫힘 시간 (ms) */
export const TOAST_DEFAULT_DURATION_MS = 3000

// ---------------------------------------------------------------------------
// Z-index 참조표 (Tailwind arbitrary value와 대응)
// ---------------------------------------------------------------------------
// z-40       Header 모바일 메뉴 오버레이
// z-[60]     ContactPanel 버튼 / 오버레이
// z-[70]     ContactPanel 패널
// z-20       BannerCarousel nav 버튼
// z-[100]    Modal (BroadcastDetailModal, DayBroadcastListModal 등)
// z-[9999]   CalendarPicker (다른 모달 위에 떠야 하는 경우)
